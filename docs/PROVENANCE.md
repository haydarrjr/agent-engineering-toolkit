# Provenance

The public repository starts a clean Git lineage rather than exposing private predecessor history.

`provenance/imports.json` records immutable source commits and treatment. Content is rewritten or adapted into the new architecture instead of importing private repository history wholesale.

The project does not vendor OpenAI's ReThinking article or GitHub documentation as project-authored text. Skills link to current official sources when current-source verification is material.
