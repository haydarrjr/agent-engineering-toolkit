# Python architecture

Respect package boundaries, import direction, supported Python versions, async/sync ownership, and public API compatibility. Prefer explicit interfaces at real boundaries over abstraction layers that only move code. Avoid circular imports and hidden import-time side effects.
