# Testing

Write tests at the lowest layer that proves the behavior. Reproduce regressions with deterministic fixtures where possible. Avoid tests that only mirror implementation details. Use integration or end-to-end coverage when the defect crosses a real boundary.
