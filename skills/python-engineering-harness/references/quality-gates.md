# Quality gates

Run the smallest affected test set while iterating. Before completion, run repository-required format/lint/type/test/package checks relevant to the changed surface. Do not claim broad health from a focused test alone.
