# Tooling

Discover project-native commands from `pyproject.toml`, lockfiles, tox/nox configs, Makefiles, CI, and contributor docs. Prefer the project's existing environment and dependency manager. Do not introduce a tool solely because this skill mentions it.
