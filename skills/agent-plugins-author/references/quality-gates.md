# Quality gates

Select gates for the changed surface: manifest parsing, required skill files, invocation policy, linked reference existence, marketplace/source reconciliation, secret hygiene, deterministic package output, and affected tests. Report the boundary of each PASS; do not infer host state.
