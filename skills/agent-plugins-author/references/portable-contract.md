# Portable contract

For Agent Plugins 1.0, keep a root `plugin.json` declaring the canonical schema and discover skills from `skills/`. Keep optional portable MCP configuration at the root when intentionally used. Validate names, versions, licenses, paths, and referenced resources. Avoid client-specific policy in the portable manifest.
