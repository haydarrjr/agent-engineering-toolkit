# Codex adapter contract

Keep Codex-native metadata as an adapter over the portable source. Version, name, repository, and license should reconcile with the root manifest. Adapter success proves only source consistency; installation and host discovery require separate evidence.
