# Implementation decisions

Define the requested outcome, affected surface, real constraints, and smallest observable completion check. Prefer a vertical change that keeps the repository runnable. Reuse existing seams and conventions before adding abstractions or dependencies. Keep unrelated cleanup out of the change.
