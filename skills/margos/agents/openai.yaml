interface:
  display_name: MARGOS
  short_description: Host-native subagent and compute-tier orchestration
  default_prompt: Use $margos only when the user explicitly requests MARGOS orchestration, compute-tier routing, or runtime-boundary work.
policy:
  allow_implicit_invocation: false
